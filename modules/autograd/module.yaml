# TinyTorch Module Metadata
# Essential system information for CLI tools and build systems

name: "autograd"
title: "Autograd"
description: "Automatic differentiation for gradient computation"
status: "not_started"  # complete, in_progress, not_started
version: "1.0.0"

# Dependencies
dependencies:
  prerequisites: ["setup", "tensor"]
  enables: ["training", "optimizers"]

# Package Export
exports_to: "tinytorch.core.autograd"

# File Structure
files:
  dev_file: "autograd_dev.py"
  test_file: "tests/test_autograd.py"
  readme: "README.md"

# Components
components:
  - "Variable"
  - "backward"
  - "grad_fn" 